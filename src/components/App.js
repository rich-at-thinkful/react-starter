var React = require('react');

require('./App.scss');

var App = function () {
    return (
        <div className="App">
            <h1>React App</h1>
        </div>
    );
};

module.exports = App;
